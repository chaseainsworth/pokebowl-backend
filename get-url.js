// Your code here.
// Note that you do NOT need the data for this function!

const getURL = function(arr) {
  return arr[1];
}

if (typeof getUrl === 'undefined') {
  getUrl = undefined;
}

console.log(getURL(['pikachu', 'https://pokeapi.co/api/v2/pokemon/25/']));

module.exports = getUrl; 
